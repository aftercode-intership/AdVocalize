# Vocalize Chat Page Error Fixes - TODO

## Status: [IN PROGRESS] 

### Step 1: [PENDING] Check Docker Services Status
```
docker-compose ps
```
Confirm postgres/redis/script-gen/backend are Up. If not: `docker-compose up -d`

### Step 2: [COMPLETE] Fix Chat Session UUID Creation ✓
- Edited `backend/internal/services/chat_service.go`: Handle nil campaign_id → NULLIF/COALESCE
- Edited `backend/internal/handlers/chat_handler.go`: Pass `nil` instead of `""`

### Step 3: [COMPLETE] Fix GLM Model Name ✓
- Edited `backend/internal/services/glm_service.go`: "glm-flash-4.7" → "glm-4-flash"

### Step 4: [PENDING] Restart Backend & Test
```
cd backend && go run main.go
```
- Test chat session creation
- Test script generation in /generate

### Step 5: [PENDING] Docker Restart & Full Test
```
docker-compose down && docker-compose up -d
```
- Test full flow: login → generate → chat

### Step 6: [PENDING] Verify Fixes
- No more UUID pq errors
- GLM "模型不存在" gone  
- Script generation succeeds (no 400/500)
- Redis cache works

**Next Action:** Run backend (`cd backend && go run main.go`), test chat creation, then Docker restart.

---
*Generated by BLACKBOXAI - Updated after each step*
